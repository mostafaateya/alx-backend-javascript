function getResponseFromAPI() {
  return new Promise((_resolve, _reject) => {
    /*eslint-disable */
    if (true) {
      _resolve();
    } else {
      _reject();
    }
    /* eslint-enable */
  });
}

export default getResponseFromAPI;
